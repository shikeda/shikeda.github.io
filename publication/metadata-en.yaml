title: KRM Documentation
subtitle: Scholarly and Technical Documentation for the KRM Database
author: Shōju Ikeda
date: "15 August 2026"
version: "Version 0.9 (complete draft)"
# Only `title` is actually read by scripts/build-krm-docx.sh (as a -M
# override, since --file-scope would otherwise let the last chapter
# file's own `title:` win). The other fields are kept here as a record
# of current values; publication/cover-en.md's own front matter is what
# actually renders the Word title page and must be kept in sync by hand.
